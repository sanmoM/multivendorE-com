import React from 'react'

export default function LoadMoreBtn({ className }) {
    return (
        <button className={`block mx-auto px-6 py-2 rounded-full bg-tertiary text-primary text-sm font-bold ${className}`}>Load More </button>
    )
}
